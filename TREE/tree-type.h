#ifndef TREE_TYPE_H
#define TREE_TYPE_H

#include <limits.h>

// Type of variables, what can be saved in tree
// -------------------------------------------------------------------------------------------------------
typedef int trv_t;
// -------------------------------------------------------------------------------------------------------
#define FORMAT_TRV "%d"
// -------------------------------------------------------------------------------------------------------
const trv_t FREE_VALUE = INT_MAX;
// -------------------------------------------------------------------------------------------------------

// Struct of node
// -------------------------------------------------------------------------------------------------------
struct node_t
{
    char bird;
    trv_t value;
    node_t* left;
    node_t* right;
};
// -------------------------------------------------------------------------------------------------------
struct tree_t
{
    node_t* root;
    size_t size;

    const char* dump_address;
};
// -------------------------------------------------------------------------------------------------------

enum sides
{
    LEFT    = 'L',
    RIGHT   = 'R'
};



#endif // TREE_TYPE_H
